# If GET /v1/admin/blogs returns 404/5xx but GET /v1/blogs works, set true to use the public list
# when not in dev. (In `npm run dev`, this fallback runs automatically.)
# VITE_USE_PUBLIC_BLOGS_FALLBACK=true
