/** Single source of truth for the Laravel backend host (no trailing slash). */
export const API_BASE_URL = "https://howeyah.subcodeco.com";

/** Axios base URL — Laravel API routes are served under `/api`. */
export const API_URL = `${API_BASE_URL}/api`;
